import Input from './Input';
import Button from './Button';
import PrivateRoute from './PrivateRoute';

export {
  Input,
  Button,
  PrivateRoute,
};

export default {
  Input,
  Button,
  PrivateRoute,
};
